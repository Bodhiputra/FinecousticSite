{
  "sections": {
    "fc_cart_redirect": {
      "type": "custom-liquid",
      "settings": {
        "custom_liquid": "{% comment %} Cart page disabled — bag uses drawer only. Redirect is in layout/theme.liquid. {% endcomment %}"
      }
    }
  },
  "order": [
    "fc_cart_redirect"
  ]
}
